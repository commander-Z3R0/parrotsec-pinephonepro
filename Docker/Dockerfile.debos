FROM godebos/debos

# Install tools required for LUKS encryption at image build time
RUN apt-get update && \
    apt-get install -y \
        cryptsetup \
        cryptsetup-bin \
        util-linux \
        e2fsprogs \
        kmod && \
    apt-get clean && \
    rm -rf /var/lib/apt/lists/*

